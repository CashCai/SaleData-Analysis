# 家电销售数据可视化看板——软件开发方案

> **一句话概括：** 一个双击就能运行的家电销售数据分析工具，导入 Excel 即可自动生成销售排行、回购分析、补贴预警和地图热力图。

---

## 目录

1. [项目概述](#1-项目概述)
2. [软件功能一览](#2-软件功能一览)
3. [技术架构](#3-技术架构)
4. [项目目录结构](#4-项目目录结构)
5. [数据库设计](#5-数据库设计)
6. [功能模块详细设计](#6-功能模块详细设计)
7. [用户界面设计](#7-用户界面设计)
8. [环境搭建与启动](#8-环境搭建与启动)
9. [数据准备指南](#9-数据准备指南)
10. [使用操作手册](#10-使用操作手册)
11. [性能优化策略](#11-性能优化策略)
12. [常见问题 FAQ](#12-常见问题-faq)
13. [开发计划与里程碑](#13-开发计划与里程碑)
14. [部署与分发](#14-部署与分发)
15. [TODO](#15. TODO)

**附录**

- [附录A：requirements.txt](#附录arequirementstxt)
- [附录B：一键启动脚本](#附录b一键启动脚本)
- [附录C：Excel 数据模板字段说明](#附录cexcel-数据模板字段说明)

---

## 1. 项目概述

### 1.1 项目定位

本软件是一个**轻量级桌面数据看板工具**，专为家电销售行业设计。使用者只需准备一份 Excel 销售数据表，导入软件后即可自动完成数据分析，生成可视化图表。

### 1.2 目标用户

| 用户角色 | 典型需求 |
|---------|---------|
| 家电卖场店长 | 查看各品牌/品类销售排名，了解热销商品 |
| 区域销售经理 | 查看区域销售热力图，对比区域业绩 |
| 财务/补贴专员 | 跟踪国家补贴核销状态，防止逾期 |
| 市场运营人员 | 分析回购顾客，评估客户忠诚度 |
| 经销商老板 | 一屏掌握整体销售情况 |

### 1.3 核心价值

- **零门槛操作**：勾选下拉即筛选，无需写公式、无需学 SQL
- **秒级出图**：导入数据后图表自动生成，不需要手动做图
- **本地运行**：数据不上传云端，信息安全
- **低配置兼容**：2GB 内存老电脑也能流畅运行

---

## 2. 软件功能一览

| 序号 | 功能 | 说明 | 面向用户 |
|------|------|------|---------|
| 1 | **Excel 数据导入** | 拖拽或点击上传 Excel 文件，自动识别字段 | 全部 |
| 2 | **综合看板** | 总销售额/订单数/回购率等核心指标卡片 + 趋势图 | 管理者 |
| 3 | **销售排行** | 按品类/品牌/型号查看销售 Top N，含环比同比 | 店长/采购 |
| 4 | **回购顾客分析** | 识别多次购买客户，回购周期计算，RFM 分层 | 运营 |
| 5 | **国补核销预警** | 到期前 7 天黄色预警、已逾期红色预警、金额统计 | 财务 |
| 6 | **销售热力图** | 省/市/县/小区级别销售地理分布，时间拖动播放 | 区域经理 |
| 7 | **数据导出** | 筛选结果和分析图表一键导出为 Excel | 全部 |

---

## 3. 技术架构

### 3.1 技术选型

| 层次 | 技术 | 为什么选它 |
|------|------|-----------|
| **界面框架** | Streamlit | 纯 Python 即可生成 Web 界面，无需写 HTML/CSS/JS |
| **文件上传** | Streamlit 原生组件 | 内置文件上传控件，支持拖拽 |
| **数据处理** | Pandas | Python 生态最成熟的数据分析库 |
| **数据库** | SQLite | 文件型数据库，零配置，与软件同目录 |
| **交互图表** | Plotly | 支持缩放、悬停提示、区域选择，比 Matplotlib 好用 |
| **中国地图** | Pyecharts | 内置中国省/市/县地图数据，支持热力图 |
| **地理编码** | 高德地图 API | 将地址文字转为经纬度，免费额度够用 |
| **打包部署** | PyInstaller | 一键打包为 .exe，发给用户双击即可运行 |

### 3.2 系统架构图

```
┌─────────────────────────────────────────────┐
│              用户浏览器（访问 localhost）      │
│             Streamlit Web 界面                │
│    [导入数据] [筛选条件] [图表展示] [导出]     │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│              业务逻辑层 (Python)              │
│                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │数据清洗  │ │销售排行  │ │回购分析  │      │
│  │& 导入    │ │计算引擎  │ │引擎      │      │
│  └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │国补预警  │ │热力图    │ │数据导出  │      │
│  │引擎      │ │生成器    │ │模块      │      │
│  └──────────┘ └──────────┘ └──────────┘      │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│            数据存储层 (SQLite)                │
│  ┌──────────────────────────────────────┐   │
│  │ sales_data.db（自动创建于软件目录）    │   │
│  │  ├── sales_orders   (销售订单表)      │   │
│  │  ├── customers      (客户信息表)      │   │
│  │  └── products       (商品信息表)       │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

### 3.3 为什么不用更复杂的技术

- **不用 MySQL/PostgreSQL**：需要单独安装数据库服务，普通用户搞不定
- **不用 React/Vue 前端**：开发周期翻倍，且需要前后端分离部署
- **不用 Java/C#**：Python + Streamlit 几十行代码就能出一个图表页
- **不用在线 BI 工具（如 FineBI、PowerBI）**：需要付费，数据上传第三方有安全顾虑

---

## 4. 项目目录结构

```
SaleData-Analysis/                 # 项目根目录
├── main.py                        # 主程序入口（Streamlit 应用）
├── requirements.txt               # Python 依赖清单
├── config.py                      # 配置文件（API Key、路径等）
├── start.bat                      # Windows 一键启动脚本
├── start.sh                       # macOS/Linux 一键启动脚本
│
├── modules/                       # 业务模块
│   ├── __init__.py
│   ├── data_import.py             # 数据导入 & 清洗
│   ├── sales_ranking.py           # 销售排行分析
│   ├── repeat_customers.py        # 回购顾客分析
│   ├── subsidy_alert.py           # 国补核销预警
│   ├── sales_heatmap.py           # 销售热力图
│   └── data_export.py             # 数据导出
│
├── database/                      # 数据库相关
│   ├── __init__.py
│   ├── models.py                  # 数据表定义
│   └── db_manager.py              # 数据库连接和操作
│
├── utils/                         # 工具函数
│   ├── __init__.py
│   ├── geo_coder.py               # 地理编码（高德API）
│   ├── data_cleaner.py            # 数据清洗规则
│   └── cache.py                   # 缓存管理
│
├── assets/                        # 静态资源
│   └── style.css                  # 自定义样式
│
├── data/                          # 用户数据目录（自动创建）
│   ├── sales_data.db              # SQLite 数据库文件
│   └── cache/                     # 缓存目录
│
└── output/                        # 导出文件目录（自动创建）
    └── exports/                   # 导出的 Excel 文件
```

> **说明**：`data/` 和 `output/` 目录由程序自动创建，用户无需手动建立。

---

## 5. 数据库设计

### 5.1 销售订单表 `sales_orders`

```sql
CREATE TABLE sales_orders (
    order_id        TEXT PRIMARY KEY,    -- 订单编号（必填，Excel导入时自动生成或使用原始单号）
    customer_name   TEXT,                -- 客户姓名
    customer_phone  TEXT,                -- 客户电话（用于识别回购客户）
    product_name    TEXT,                -- 商品名称
    category        TEXT,                -- 品类（如：空调、冰箱、洗衣机）
    brand           TEXT,                -- 品牌（如：美的、格力、海尔）
    model           TEXT,                -- 型号
    quantity        INTEGER DEFAULT 1,   -- 数量
    amount          REAL,                -- 销售金额（元）
    subsidy_amount  REAL DEFAULT 0,      -- 国补金额（元）
    subsidy_status  TEXT DEFAULT '未核销', -- 核销状态：'已核销' / '未核销'
    sale_date       DATE,                -- 销售日期（必填）
    channel         TEXT,                -- 销售渠道：'门店' / '线上' / '分销'
    address         TEXT,                -- 客户地址（用于地理编码）
    geo_code        TEXT                 -- 经纬度（程序自动补全，格式："经度,纬度"）
);

-- 索引建议：常作为筛选条件的字段建索引
CREATE INDEX idx_sale_date ON sales_orders(sale_date);
CREATE INDEX idx_category ON sales_orders(category);
CREATE INDEX idx_brand ON sales_orders(brand);
CREATE INDEX idx_customer_phone ON sales_orders(customer_phone);
CREATE INDEX idx_subsidy_status ON sales_orders(subsidy_status);
```

### 5.2 客户汇总表 `customers`

```sql
CREATE TABLE customers (
    customer_phone     TEXT PRIMARY KEY,   -- 客户电话（唯一标识）
    customer_name      TEXT,               -- 客户姓名
    first_purchase     DATE,               -- 首次购买日期
    last_purchase      DATE,               -- 最近购买日期
    purchase_count     INTEGER DEFAULT 1,  -- 累计购买次数
    total_amount       REAL,               -- 累计消费金额
    avg_purchase_cycle INTEGER,            -- 平均回购周期（天）
    rfm_score          TEXT                -- RFM分层：'高价值' / '重要发展' / '重要保持' / '一般'
);

CREATE INDEX idx_purchase_count ON customers(purchase_count);
```

### 5.3 商品信息表 `products`

```sql
CREATE TABLE products (
    product_id      TEXT PRIMARY KEY,       -- 商品ID（可用"品牌+型号"拼接）
    product_name    TEXT,                   -- 商品名称
    category        TEXT,                   -- 品类
    brand           TEXT,                   -- 品牌
    model           TEXT,                   -- 型号
    sale_price      REAL,                   -- 销售单价
    total_sales     INTEGER DEFAULT 0,      -- 累计销量（程序自动更新）
    total_revenue   REAL DEFAULT 0          -- 累计销售额（程序自动更新）
);

CREATE INDEX idx_product_category ON products(category);
CREATE INDEX idx_product_brand ON products(brand);
```

---

## 6. 功能模块详细设计

### 6.1 数据导入模块

#### 功能描述

- 支持拖拽或点击上传 `.xlsx` / `.xls` 文件
- 自动识别表头字段（支持常见别名，如"金额"可匹配"销售额""成交价"等）
- 数据校验：必填字段缺失提示、日期格式修正、金额字段去除非数字字符
- 导入完成后显示预览（前 5 行）+ 统计摘要（总行数、日期范围、品类数）

#### 关键逻辑

```
上传文件 → 识别表头 → 字段映射 → 数据清洗 → 写入SQLite → 显示预览
```

#### 核心代码（`modules/data_import.py`）

```python
import pandas as pd
import streamlit as st
import sqlite3
from datetime import datetime

# 字段别名映射表：Excel列名 → 标准列名
FIELD_MAP = {
    '订单编号': 'order_id', '订单号': 'order_id', '单号': 'order_id',
    '客户姓名': 'customer_name', '顾客姓名': 'customer_name', '姓名': 'customer_name',
    '客户电话': 'customer_phone', '电话': 'customer_phone', '手机号': 'customer_phone',
    '商品名称': 'product_name', '产品名称': 'product_name', '商品': 'product_name',
    '品类': 'category', '类别': 'category', '产品类别': 'category',
    '品牌': 'brand',
    '型号': 'model', '规格型号': 'model',
    '数量': 'quantity', '销量': 'quantity',
    '金额': 'amount', '销售金额': 'amount', '销售额': 'amount', '成交价': 'amount',
    '国补金额': 'subsidy_amount', '补贴金额': 'subsidy_amount', '补贴': 'subsidy_amount',
    '核销状态': 'subsidy_status', '补贴状态': 'subsidy_status',
    '销售日期': 'sale_date', '日期': 'sale_date', '购买日期': 'sale_date',
    '渠道': 'channel', '销售渠道': 'channel',
    '地址': 'address', '客户地址': 'address', '收货地址': 'address',
}

REQUIRED_FIELDS = ['sale_date', 'amount']


def normalize_columns(df: pd.DataFrame) -> pd.DataFrame:
    """将Excel表头映射为标准列名"""
    df = df.rename(columns=FIELD_MAP)
    return df


def clean_data(df: pd.DataFrame) -> pd.DataFrame:
    """数据清洗"""
    # 去除完全空行
    df = df.dropna(how='all')

    # 日期字段标准化
    if 'sale_date' in df.columns:
        df['sale_date'] = pd.to_datetime(df['sale_date'], errors='coerce')

    # 金额字段：移除非数字字符（￥、逗号等）
    for col in ['amount', 'subsidy_amount']:
        if col in df.columns:
            df[col] = df[col].astype(str).str.replace(r'[^\d.]', '', regex=True)
            df[col] = pd.to_numeric(df[col], errors='coerce').fillna(0)

    # 数量默认为1
    if 'quantity' in df.columns:
        df['quantity'] = pd.to_numeric(df['quantity'], errors='coerce').fillna(1)

    # 补全默认值
    if 'subsidy_status' not in df.columns:
        df['subsidy_status'] = '未核销'

    if 'channel' not in df.columns:
        df['channel'] = '门店'

    return df


def validate_data(df: pd.DataFrame) -> tuple[bool, list[str]]:
    """校验数据，返回(是否通过, 错误列表)"""
    errors = []
    for field in REQUIRED_FIELDS:
        if field not in df.columns:
            errors.append(f"缺少必填字段：{field}")
        else:
            missing = df[field].isna().sum()
            if missing > 0:
                errors.append(f"字段「{field}」有 {missing} 行数据为空")
    return len(errors) == 0, errors


def import_to_db(df: pd.DataFrame, db_path: str = "data/sales_data.db"):
    """将数据写入SQLite"""
    conn = sqlite3.connect(db_path)
    df.to_sql('sales_orders', conn, if_exists='replace', index=False)
    conn.close()


def run_import_ui():
    """Streamlit 导入界面"""
    st.subheader("📥 数据导入")

    uploaded_file = st.file_uploader(
        "拖拽 Excel 文件到这里，或点击浏览",
        type=["xlsx", "xls"],
        help="支持 .xlsx 和 .xls 格式"
    )

    if uploaded_file is not None:
        try:
            df = pd.read_excel(uploaded_file)
            st.info(f"已读取 {len(df)} 行数据，{len(df.columns)} 个字段")

            df = normalize_columns(df)
            df = clean_data(df)

            passed, errors = validate_data(df)
            if not passed:
                for e in errors:
                    st.warning(e)
            else:
                import_to_db(df)
                st.success(f"✅ 导入成功！共 {len(df)} 条记录")

                # 预览
                st.write("**数据预览（前5行）：**")
                st.dataframe(df.head(5))

        except Exception as e:
            st.error(f"导入失败：{str(e)}")
```

---

### 6.2 筛选模块

#### 设计理念

原方案中的"拖拽标签筛选"操作门槛较高，本方案改为 **侧边栏勾选 + 下拉选择** 的方式，与常见电商网站的筛选体验一致。

#### 筛选控件设计

| 维度 | 控件类型 | 说明 |
|------|---------|------|
| 时间范围 | 双日期选择器（开始 → 结束） | 默认最近一年 |
| 品类 | 多选框 | 如：空调、冰箱、洗衣机… |
| 品牌 | 多选框（随品类联动） | 选择品类后自动过滤品牌列表 |
| 型号 | 搜索下拉框 | 支持输入关键词搜索 |
| 地区 | 级联下拉（省→市→县） | 逐级联动 |
| 核销状态 | 单选：全部 / 已核销 / 未核销 / 已逾期 | 用于国补预警 |
| 客户类型 | 单选：全部 / 新客户 / 回购客户 | 用于回购分析 |

#### 交互逻辑

```
用户调整任意筛选条件 → 所有图表实时刷新
```

**防抖处理**：日期选择器等需要确认的控件，点"应用筛选"按钮后再刷新（避免频繁重算）。

#### 筛选条件保存

- 每个页面独立的筛选条件会保存在浏览器 Session 中
- 支持"保存当前筛选方案"为快捷选项（如"2025年1季度-空调-美的"）

#### 核心代码（简化版）

```python
import streamlit as st
import pandas as pd


def render_filters(df: pd.DataFrame) -> dict:
    """渲染筛选控件，返回筛选条件字典"""
    filters = {}

    with st.sidebar:
        st.header("🔍 筛选条件")

        # 时间范围
        if 'sale_date' in df.columns:
            min_date = df['sale_date'].min().date()
            max_date = df['sale_date'].max().date()
            date_range = st.date_input(
                "销售日期范围",
                value=(min_date, max_date),
                min_value=min_date,
                max_value=max_date
            )
            if len(date_range) == 2:
                filters['date_start'] = date_range[0]
                filters['date_end'] = date_range[1]

        # 品类（多选）
        if 'category' in df.columns:
            categories = sorted(df['category'].dropna().unique())
            selected_cat = st.multiselect("品类", categories, default=categories)
            if selected_cat:
                filters['categories'] = selected_cat

        # 品牌（联动过滤）
        if 'brand' in df.columns:
            brand_df = df
            if 'categories' in filters:
                brand_df = brand_df[brand_df['category'].isin(filters['categories'])]
            brands = sorted(brand_df['brand'].dropna().unique())
            filters['brands'] = st.multiselect("品牌", brands, default=brands)

        # 核销状态
        if 'subsidy_status' in df.columns:
            filters['subsidy_status'] = st.radio(
                "核销状态",
                ['全部', '已核销', '未核销', '已逾期'],
                horizontal=True
            )

        # 应用按钮
        st.button("🔄 应用筛选", type="primary", use_container_width=True)

    return filters


def apply_filters(df: pd.DataFrame, filters: dict) -> pd.DataFrame:
    """将筛选条件应用到数据"""
    result = df.copy()

    if 'date_start' in filters and 'sale_date' in result.columns:
        result = result[result['sale_date'] >= pd.Timestamp(filters['date_start'])]
    if 'date_end' in filters and 'sale_date' in result.columns:
        result = result[result['sale_date'] <= pd.Timestamp(filters['date_end'])]
    if 'categories' in filters and 'category' in result.columns:
        result = result[result['category'].isin(filters['categories'])]
    if 'brands' in filters and 'brand' in result.columns:
        result = result[result['brand'].isin(filters['brands'])]

    return result
```

---

### 6.3 核心指标看板

#### 功能描述

页面顶部一行指标卡片，一眼看完关键数据：

| 指标 | 计算方式 | 说明 |
|------|---------|------|
| 总销售额 | SUM(amount) | 筛选范围内的总销售额 |
| 总订单数 | COUNT(order_id) | 筛选范围内的订单总数 |
| 平均客单价 | 总销售额 / 总订单数 | 单均消费金额 |
| 回购客户数 | COUNT(购买次数≥2的客户) | 多次购买的客户数 |
| 回购率 | 回购客户数 / 总客户数 | 客户回头率 |
| 国补待核销额 | SUM(未核销补贴) | 金额跟进 |

#### UI 渲染（Streamlit columns + metric）

```python
def render_kpi_cards(df: pd.DataFrame):
    """渲染核心指标卡片"""
    total_sales = df['amount'].sum()
    total_orders = len(df)
    avg_price = total_sales / total_orders if total_orders > 0 else 0

    # 回购计算
    customer_counts = df.groupby('customer_phone')['order_id'].count()
    repeat_count = (customer_counts >= 2).sum()
    repeat_rate = repeat_count / len(customer_counts) if len(customer_counts) > 0 else 0

    # 补贴
    pending_subsidy = df[df['subsidy_status'] == '未核销']['subsidy_amount'].sum()

    col1, col2, col3, col4, col5, col6 = st.columns(6)

    col1.metric("总销售额", f"¥{total_sales:,.0f}")
    col2.metric("总订单数", f"{total_orders:,}")
    col3.metric("平均客单价", f"¥{avg_price:,.0f}")
    col4.metric("回购客户数", str(repeat_count))
    col5.metric("回购率", f"{repeat_rate:.1%}")
    col6.metric("待核销补贴", f"¥{pending_subsidy:,.0f}")
```

---

### 6.4 销售排行模块

#### 功能描述

- 按品类 / 品牌 / 型号三个层级查看销售排名
- 默认显示 Top 20，可调整
- 支持按销售额或销售量排序
- 附加热销标记（排名前 10% 标🔥）

#### 关键代码（`modules/sales_ranking.py`）

```python
import pandas as pd
import streamlit as st
import plotly.express as px


def generate_ranking(df: pd.DataFrame, group_by: str = 'brand',
                     sort_by: str = 'amount', top_n: int = 20) -> pd.DataFrame:
    """
    生成销售排行
    group_by: 'category' / 'brand' / 'model'
    sort_by: 'amount'（销售额）/ 'quantity'（销量）
    """
    agg_dict = {
        'amount': 'sum',
        'order_id': 'count',
    }
    if 'quantity' in df.columns:
        agg_dict['quantity'] = 'sum'

    ranking = df.groupby(group_by).agg(agg_dict).sort_values(sort_by, ascending=False)

    ranking = ranking.head(top_n).reset_index()
    ranking.columns = [group_by, '销售额', '订单数', '销量'][:len(ranking.columns)]
    ranking['销售额占比'] = (ranking['销售额'] / ranking['销售额'].sum() * 100).round(1)
    ranking['排名'] = range(1, len(ranking) + 1)

    return ranking


def render_ranking_ui(df: pd.DataFrame):
    """排行页面 UI"""
    st.subheader("🏆 销售排行")

    col1, col2, col3 = st.columns(3)
    with col1:
        group_by = st.selectbox("排行维度", ['category', 'brand', 'model'],
                                format_func=lambda x: {'category': '品类', 'brand': '品牌', 'model': '型号'}.get(x, x))
    with col2:
        sort_by = st.selectbox("排序依据", ['amount', 'quantity'],
                               format_func=lambda x: '销售额' if x == 'amount' else '销售量')
    with col3:
        top_n = st.slider("显示数量", 5, 50, 20)

    ranking = generate_ranking(df, group_by, sort_by, top_n)

    # 横向柱状图
    fig = px.bar(
        ranking,
        x='销售额',
        y=ranking.columns[0],
        orientation='h',
        text='销售额占比',
        color='销售额',
        color_continuous_scale='Blues',
        title=f"{group_by} 销售排行 Top {top_n}"
    )
    fig.update_layout(yaxis={'categoryorder': 'total ascending'})
    st.plotly_chart(fig, use_container_width=True)

    # 数据表
    st.dataframe(ranking, use_container_width=True)
```

---

### 6.5 回购顾客分析模块

#### 功能描述

- 识别购买次数 ≥ 2 的顾客
- 计算每人平均回购周期（两次购买间隔天数）
- RFM 模型分层（最近购买时间、购买频率、消费金额）
- 按回购次数和累计消费排序展示

#### 业务规则

| 指标 | 计算方式 |
|------|---------|
| 回购次数 | 该顾客订单总数 |
| 回购周期 | (最近购买日期 - 最早购买日期) / (购买次数 - 1) |
| RFM-R（近度） | 最近一次购买距今天数 |
| RFM-F（频度） | 购买总次数 |
| RFM-M（金额） | 累计消费总额 |

#### RFM 分层标准

| 层级 | 判定 | 策略建议 |
|------|------|---------|
| 🥇 高价值客户 | R低 + F高 + M高 | 重点维护，主动联系 |
| 🥈 重要保持 | F低 + M高 | 定期回访，促进复购 |
| 🥉 重要发展 | R低 + M高 + F低 | 新晋优质客户，提升频次 |
| 一般客户 | 其余 | 标准服务 |

#### 核心代码（`modules/repeat_customers.py`）

```python
import pandas as pd
import streamlit as st
import plotly.express as px


def analyze_repeat_customers(df: pd.DataFrame) -> pd.DataFrame:
    """回购客户分析（修正原方案中聚合后列引用错误）"""
    if 'customer_phone' not in df.columns or 'sale_date' not in df.columns:
        return pd.DataFrame()

    customer_stats = df.groupby('customer_phone').agg(
        购买次数=('order_id', 'count'),
        累计消费=('amount', 'sum'),
        最早购买=('sale_date', 'min'),
        最近购买=('sale_date', 'max'),
    ).reset_index()

    # 仅保留回购客户（购买次数 >= 2）
    repeat = customer_stats[customer_stats['购买次数'] >= 2].copy()

    if len(repeat) == 0:
        return repeat

    # 回购周期 = 时间跨度 / (次数 - 1)
    time_span = (repeat['最近购买'] - repeat['最早购买']).dt.days
    repeat['回购周期(天)'] = (time_span / (repeat['购买次数'] - 1)).round(1)

    # RFM 打分
    today = pd.Timestamp.now()
    repeat['R_天数'] = (today - repeat['最近购买']).dt.days

    def rfm_label(row):
        if row['R_天数'] <= 90 and row['购买次数'] >= 3 and row['累计消费'] >= 10000:
            return '高价值客户'
        elif row['累计消费'] >= 10000:
            return '重要保持'
        elif row['R_天数'] <= 90 and row['累计消费'] >= 5000:
            return '重要发展'
        return '一般客户'

    repeat['RFM分层'] = repeat.apply(rfm_label, axis=1)
    repeat = repeat.sort_values('累计消费', ascending=False)

    return repeat


def render_repeat_customers_ui(df: pd.DataFrame):
    """回购分析页面"""
    st.subheader("🔄 回购顾客分析")

    result = analyze_repeat_customers(df)

    if len(result) == 0:
        st.info("当前筛选条件下没有回购客户（购买次数≥2）")
        return

    # 概览指标
    total_customers = df['customer_phone'].nunique()
    repeat_count = len(result)
    repeat_rate = repeat_count / total_customers if total_customers > 0 else 0

    col1, col2, col3 = st.columns(3)
    col1.metric("总客户数", str(total_customers))
    col2.metric("回购客户数", str(repeat_count))
    col3.metric("回购率", f"{repeat_rate:.1%}")

    # RFM 分层饼图
    fig_pie = px.pie(
        result, names='RFM分层', title='RFM 客户分层分布',
        color='RFM分层',
        color_discrete_map={
            '高价值客户': '#2ecc71',
            '重要保持': '#3498db',
            '重要发展': '#f39c12',
            '一般客户': '#95a5a6'
        }
    )
    st.plotly_chart(fig_pie, use_container_width=True)

    # 客户列表
    st.dataframe(
        result[['customer_phone', '购买次数', '累计消费', '回购周期(天)', 'RFM分层']],
        use_container_width=True
    )
```

---

### 6.6 国补核销预警模块

#### 功能描述

国家家电补贴政策下，部分订单享受补贴但需在规定时间内完成核销。此模块自动追踪核销状态并提供分级预警。

#### 预警规则

| 状态 | 条件 | 颜色 | 操作建议 |
|------|------|------|---------|
| ✅ 已核销 | `subsidy_status = '已核销'` | 绿色 | 无需处理 |
| ⚠️ 即将到期 | 未核销 且 距到期 ≤ 7天 | 黄色 | 优先办理 |
| 🔴 已逾期 | 未核销 且 距到期 < 0天 | 红色 | 紧急处理 |
| ⏳ 待核销 | 未核销 且 距到期 > 7天 | 灰色 | 按进度跟进 |

**核销期限**：默认销售日期 + 30天（可通过 `config.py` 调整）

#### 核心代码（`modules/subsidy_alert.py`）

```python
import pandas as pd
import streamlit as st
from datetime import datetime, timedelta


def check_subsidy_alerts(df: pd.DataFrame, deadline_days: int = 30) -> pd.DataFrame:
    """检查国补核销状态，返回带预警标记的数据"""
    alert_df = df[df['subsidy_amount'] > 0].copy()

    if len(alert_df) == 0:
        return alert_df

    today = pd.Timestamp.now().normalize()
    alert_df['核销截止日'] = alert_df['sale_date'] + pd.Timedelta(days=deadline_days)
    alert_df['剩余天数'] = (alert_df['核销截止日'] - today).dt.days

    def alert_level(days, status):
        if status == '已核销':
            return '✅ 已核销'
        if days < 0:
            return '🔴 已逾期'
        if days <= 7:
            return '⚠️ 即将到期'
        return '⏳ 待核销'

    alert_df['预警状态'] = alert_df.apply(
        lambda r: alert_level(r['剩余天数'], r['subsidy_status']), axis=1
    )

    return alert_df.sort_values('剩余天数')


def render_subsidy_alert_ui(df: pd.DataFrame):
    """国补预警页面"""
    st.subheader("🚨 国补核销预警")

    # 可调整的核销期限
    deadline = st.sidebar.number_input("核销期限（天）", min_value=7, max_value=90, value=30)

    alert_df = check_subsidy_alerts(df, deadline)

    if len(alert_df) == 0:
        st.info("当前数据中没有享受国补的订单")
        return

    # 统计卡片
    total_subsidy = alert_df['subsidy_amount'].sum()
    overdue = alert_df[alert_df['预警状态'] == '🔴 已逾期']
    urgent = alert_df[alert_df['预警状态'] == '⚠️ 即将到期']

    col1, col2, col3, col4 = st.columns(4)
    col1.metric("国补订单总数", f"{len(alert_df)}")
    col2.metric("补贴总金额", f"¥{total_subsidy:,.0f}")
    col3.metric("🔴 已逾期", f"{len(overdue)} 单", delta=f"¥{overdue['subsidy_amount'].sum():,.0f}")
    col4.metric("⚠️ 即将到期", f"{len(urgent)} 单", delta=f"¥{urgent['subsidy_amount'].sum():,.0f}")

    # 预警列表
    st.write("**预警明细：**")
    display_cols = ['order_id', 'customer_name', 'product_name', 'amount',
                    'subsidy_amount', 'sale_date', '核销截止日', '剩余天数', '预警状态']
    st.dataframe(
        alert_df[[c for c in display_cols if c in alert_df.columns]],
        use_container_width=True,
        column_config={
            '预警状态': st.column_config.TextColumn(width='medium')
        }
    )
```

---

### 6.7 销售热力图模块

#### 功能描述

- 在地图上展示销售额/销售量的地理分布
- 支持省 → 市 → 县 三级下钻
- 支持双指标切换（销售额 / 销售量）
- 悬停显示具体数据（地名 + 销售额）

#### 地理编码

- 使用高德地图 API 将地址文本转为经纬度
- 首次转换后缓存到 SQLite，避免重复调用
- 免费 API 额度（5000次/天）足够日常使用

#### 核心代码（`modules/sales_heatmap.py`）

```python
import pandas as pd
import streamlit as st
import plotly.express as px

# 优先使用 Plotly 而非 Pyecharts，因为：
# 1. Plotly 与 Streamlit 集成更紧密
# 2. 不需要额外加载地图资源文件
# 3. 缩放交互更流畅


def create_heatmap(df: pd.DataFrame, geo_level: str = 'province',
                   metric: str = 'amount') -> px.choropleth:
    """
    创建销售热力图
    geo_level: 'province' / 'city' / 'district'
    metric: 'amount' / 'quantity'
    """

    # 按地理层级聚合
    if geo_level not in df.columns:
        st.warning(f"数据中不含「{geo_level}」字段，无法生成地图")
        return None

    geo_data = df.groupby(geo_level).agg(
        销售额=('amount', 'sum'),
        销售量=('quantity', 'sum') if 'quantity' in df.columns else ('order_id', 'count')
    ).reset_index()

    metric_label = '销售额' if metric == 'amount' else '销售量'

    # 使用 Plotly 的中国地图
    # 注：需数据中包含标准省份名称（如"广东省"而非"广东"）
    fig = px.choropleth(
        geo_data,
        locations=geo_level,
        locationmode='geojson-id' if geo_level == 'district' else None,
        color=metric_label,
        hover_name=geo_level,
        color_continuous_scale='OrRd',
        title=f"{metric_label} 地理分布",
    )

    # 中国地图范围设置
    fig.update_geos(
        visible=False,
        lonaxis_range=[73, 135],
        lataxis_range=[18, 53],
    )

    fig.update_layout(height=600)
    return fig


def render_heatmap_ui(df: pd.DataFrame):
    """热力图页面"""
    st.subheader("🗺️ 销售热力图")

    col1, col2 = st.columns(2)
    with col1:
        geo_level = st.selectbox(
            "地理层级",
            options=[c for c in ['province', 'city', 'district'] if c in df.columns],
            format_func=lambda x: {'province': '省', 'city': '市', 'district': '县/区'}.get(x, x)
        )
    with col2:
        metric = st.selectbox(
            "指标",
            options=['amount', 'quantity'],
            format_func=lambda x: '销售额' if x == 'amount' else '销售量'
        )

    fig = create_heatmap(df, geo_level, metric)
    if fig:
        st.plotly_chart(fig, use_container_width=True)
```

---

### 6.8 数据导出模块

```python
import streamlit as st
import pandas as pd
from io import BytesIO


def export_to_excel(df: pd.DataFrame, filename: str = "分析结果") -> BytesIO:
    """将 DataFrame 导出为 Excel 字节流"""
    output = BytesIO()
    with pd.ExcelWriter(output, engine='openpyxl') as writer:
        df.to_excel(writer, sheet_name='数据', index=False)
    output.seek(0)
    return output


def render_export_button(df: pd.DataFrame):
    """导出按钮"""
    buf = export_to_excel(df)
    st.download_button(
        label="📥 导出当前数据为 Excel",
        data=buf,
        file_name=f"销售分析导出_{pd.Timestamp.now().strftime('%Y%m%d')}.xlsx",
        mime="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    )
```

---

## 7. 用户界面设计

### 7.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  🏠 家电销售数据可视化看板              [📥 导入数据] [❓ 帮助] │
├──────────────┬──────────────────────────────────────────────┤
│  🔍 筛选条件 │  📊 核心指标卡片                              │
│              │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌─────┐│
│  销售日期    │  │总销售│ │总订单│ │客单价│ │回购率│ │待核销││
│  [2024-01]  │  │¥128万│ │3,421 │ │¥374  │ │23.5% │ │¥2.1万││
│  [2025-05]  │  └──────┘ └──────┘ └──────┘ └──────┘ └─────┘│
│              │                                              │
│  品类        │  ┌──────────────────┐ ┌──────────────────┐  │
│  ☑ 空调     │  │ 🏆 销售排行 Top20│ │ 🔄 回购顾客分析   │  │
│  ☑ 冰箱     │  │ [柱状图]         │ │ [饼图 + 表格]    │  │
│  ☑ 洗衣机   │  │                  │ │                  │  │
│  ☐ 电视     │  └──────────────────┘ └──────────────────┘  │
│              │                                              │
│  品牌        │  ┌──────────────────┐ ┌──────────────────┐  │
│  ☑ 美的     │  │ 🚨 国补核销预警  │ │ 📋 数据明细      │  │
│  ☑ 格力     │  │ [统计卡 + 列表]  │ │ [可排序表格]     │  │
│  ☑ 海尔     │  │                  │ │                  │  │
│              │  └──────────────────┘ └──────────────────┘  │
│  核销状态    │                                              │
│  ○ 全部     │  ┌──────────────────────────────────────────┐│
│  ○ 已核销   │  │ 🗺️ 销售热力图                           ││
│  ○ 未核销   │  │ [中国地图 - 颜色深浅表示销售密集度]       ││
│              │  │                                          ││
│  [🔄 应用]   │  └──────────────────────────────────────────┘│
└──────────────┴──────────────────────────────────────────────┘
```

### 7.2 页面结构（侧边栏导航）

```
首页
├── 📊 综合看板（默认）
├── 🏆 销售排行
├── 🔄 回购顾客分析
├── 🚨 国补核销预警
├── 🗺️ 销售热力图
└── ℹ️ 帮助 & 关于
```

### 7.3 交互原则

- **筛选即刷新**：侧边栏筛选条件变更 → 主内容区图表实时更新
- **图表联动**：点击排行中的某个品牌 → 其他图表自动过滤为该品牌数据
- **数据下钻**：地图点击省份 → 下钻到市；排行点击品类 → 下钻到品牌
- **自适应布局**：不使用拖拽布局，固定卡片位置，降低操作学习成本

---

## 8. 环境搭建与启动

### 8.1 运行环境要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 7 / macOS 10.12 / Ubuntu 18.04 | Windows 10+ / macOS 12+ |
| 内存 | 2 GB | 4 GB+ |
| 硬盘 | 500 MB | 1 GB+ |
| Python | 3.8+ | 3.10+ |

### 8.2 安装步骤（开发者）

```bash
# 1. 进入项目目录
cd SaleData-Analysis

# 2. 创建虚拟环境（可选但推荐）
python -m venv venv

# 3. 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 4. 安装依赖
pip install -r requirements.txt

# 5. 启动应用
streamlit run main.py

# 6. 浏览器自动打开 http://localhost:8501
```

### 8.3 一键启动（非技术人员）

**Windows 用户**：双击 `start.bat`

**macOS/Linux 用户**：终端执行 `bash start.sh`

### 8.4 打包为独立 EXE

```bash
# 安装打包工具
pip install pyinstaller

# 打包为单个 exe 文件
pyinstaller --onefile --name "家电销售看板" main.py

# 分发给用户时，连同 data/ 目录一起打包为 zip
# 用户解压后双击 家电销售看板.exe 即可运行
```

---

## 9. 数据准备指南

### 9.1 Excel 模板

用户需要准备一份包含以下字段的 Excel 文件（`.xlsx` 或 `.xls`）：

| 字段 | 是否必填 | 说明 | 示例 |
|------|---------|------|------|
| `销售日期` | ✅ 必填 | 格式 YYYY-MM-DD | 2025-03-15 |
| `金额` | ✅ 必填 | 销售金额（元） | 2999 |
| `订单编号` | 推荐 | 不填则自动生成 | D20250315001 |
| `客户姓名` | 推荐 | 分析回购需要 | 张三 |
| `客户电话` | 推荐 | 唯一标识客户 | 13800138000 |
| `商品名称` | 推荐 | 产品名 | 美的1.5匹变频空调 |
| `品类` | 推荐 | 用于分类排行 | 空调 |
| `品牌` | 推荐 | 用于品牌排行 | 美的 |
| `型号` | 可选 | 产品具体型号 | KFR-35GW |
| `数量` | 可选 | 默认1 | 1 |
| `国补金额` | 可选 | 涉及补贴需填 | 300 |
| `核销状态` | 可选 | 已核销 / 未核销 | 未核销 |
| `销售渠道` | 可选 | 门店 / 线上 / 分销 | 门店 |
| `地址` | 可选 | 用于热力图，越详细越好 | 广东省广州市天河区XX小区 |

### 9.2 数据要求

- Excel 第一行必须是表头（字段名）
- 表头名称无需完全一致，系统会自动匹配（如"销售额""成交价""金额"都可识别为金额字段）
- 日期建议用 `YYYY-MM-DD` 格式，避免 `2025.3.15` 这种写法
- 金额用纯数字，不要带"元"字或"￥"符号（系统也会自动清理，但纯数字最可靠）
- 建议每次导入的数据量不超过 10 万行（超过也能处理，但加载稍慢）

### 9.3 示例数据（前 3 行）

| 销售日期 | 订单编号 | 客户姓名 | 客户电话 | 商品名称 | 品类 | 品牌 | 型号 | 数量 | 金额 | 国补金额 | 核销状态 | 销售渠道 | 地址 |
|---------|---------|---------|---------|---------|------|------|------|------|------|---------|---------|---------|------|
| 2025-03-15 | D001 | 张三 | 13800138001 | 格力1.5匹空调 | 空调 | 格力 | KFR-35GW | 1 | 3299 | 300 | 未核销 | 门店 | 广东省广州市天河区 |
| 2025-03-16 | D002 | 李四 | 13900139002 | 海尔冰箱 | 冰箱 | 海尔 | BCD-500 | 1 | 4599 | 0 | 未核销 | 门店 | 广东省深圳市南山区 |
| 2025-03-17 | D003 | 张三 | 13800138001 | 美的洗衣机 | 洗衣机 | 美的 | MG100 | 1 | 2599 | 200 | 已核销 | 线上 | 广东省广州市天河区 |

---

## 10. 使用操作手册

### 步骤 1：启动软件

- **Windows**：双击 `start.bat` 或 `家电销售看板.exe`
- 等待几秒钟，会自动弹出浏览器窗口，地址栏显示 `http://localhost:8501`
- 如果没有自动弹出，手动打开浏览器输入 `http://localhost:8501`

### 步骤 2：导入数据

1. 点击页面顶部的 **📥 导入数据** 按钮，或直接在首页拖拽区域上传 Excel 文件
2. 系统会自动读取文件，显示行数和字段名
3. 检查右侧预览区域，确认数据正确
4. 如果有字段匹配失败的警告，检查 Excel 表头名称是否清晰

### 步骤 3：筛选和分析

1. 在左侧边栏选择筛选条件（只需勾选/下拉，无需任何代码）
2. 点击 **🔄 应用筛选**，所有图表自动刷新
3. 通过左侧导航切换不同分析页面：
   - 综合看板：看总体指标
   - 销售排行：哪个品牌/品类卖得好
   - 回购分析：哪些客户是回头客
   - 国补预警：有哪些补贴还没核销
   - 热力图：哪个地方卖得多

### 步骤 4：导出结果

1. 在任何页面底部点击 **📥 导出当前数据为 Excel**
2. 文件会下载到浏览器默认下载目录
3. 导出的文件包含当前筛选条件下的数据，可以直接在 Excel 中打开编辑

---

## 11. 性能优化策略

### 11.1 针对大数据量（10万+ 行）

| 策略 | 说明 |
|------|------|
| **SQLite 查询优先** | 筛选和聚合在数据库层完成，不全量加载到内存 |
| **数据采样** | 热力图数据点超过 5000 时自动做聚类采样 |
| **分页加载** | 明细表格每次只加载 200 行，滚动时动态加载 |
| **计算结果缓存** | 同一筛选条件下不重复计算，30 秒内直接返回缓存 |

### 11.2 Pandas 数据类型优化

```python
# 优化内存占用
df['category'] = df['category'].astype('category')
df['brand'] = df['brand'].astype('category')
df['subsidy_status'] = df['subsidy_status'].astype('category')
df['amount'] = pd.to_numeric(df['amount'], downcast='float')
df['quantity'] = pd.to_numeric(df['quantity'], downcast='integer')
```

### 11.3 图表渲染优化

- Plotly 图表使用 `webgl` 渲染后端（大数据散点图）
- 热力图数据点超过阈值的自动聚合为区域块

---

## 12. 常见问题 FAQ

### Q1：Excel 导入后提示"字段匹配失败"

**原因**：Excel 表头名称与系统识别的名称差异较大。

**解决**：
- 将表头改为中文常用名（如"销售日期""金额""品牌"等）
- 或查看程序输出的识别报告，手动修改对应列名后再导入

### Q2：地图不显示

**原因**：Plotly 的地图需要网络加载底图资源。

**解决**：
- 确保电脑可以访问外网
- 如果公司网络限制，可以改用 Pyecharts 本地地图（需在环境搭建时安装 `pyecharts` 和地图资源包）

### Q3：导入新数据后看板没变化

**原因**：导入了新文件但筛选条件排除了新数据。

**解决**：
- 检查侧边栏日期范围是否包含新数据
- 点击 **🔄 应用筛选** 刷新

### Q4：软件运行越来越慢

**原因**：缓存数据积累。

**解决**：
- 重启软件即可清空内存缓存
- 如果数据量确实很大（超过 50 万行），建议按月分文件导入

### Q5：可以多人同时使用吗

当前版本是单机版，一个电脑开一个窗口使用。如果需要多人使用，可以将数据库文件 `sales_data.db` 放在共享文件夹中，每个人从各自电脑连接同一个数据库（需额外配置）。

---

## 13. 开发计划与里程碑

| 阶段 | 时间 | 任务 | 可交付物 |
|------|------|------|---------|
| **第一阶段** | 第 1-2 周 | 项目骨架搭建 | 可运行的 Streamlit 空白应用 + 数据库初始化 + 数据导入功能可用 |
| **第二阶段** | 第 3-5 周 | 核心功能开发 | 销售排行模块 + 回购分析模块 + 国补预警模块 + 综合看板 |
| **第三阶段** | 第 6-7 周 | 可视化和交互 | 热力图模块 + 筛选联动 + 界面美化 + 数据导出 |
| **第四阶段** | 第 8 周 | 测试和部署 | 功能测试 + 性能测试 + 用户文档 + PyInstaller 打包 + 交付包 |

**每个阶段结束的检查点：**
- 功能是否按预期运行（用真实 Excel 数据测试）
- 代码是否通过 `pytest` 单元测试
- 非技术人员能否独立完成操作（找一个人实际试用）

---

## 14. 部署与分发

### 14.0 便携版 Python + start.bat 方案（已采用）

完成。Python 3.8.10 x64 便携版已打包好，总大小 430MB，包含版本：

  ┌───────────┬────────┐
  │    包     │  版本  │
  ├───────────┼────────┤
  │ Python    │ 3.8.10 │
  ├───────────┼────────┤
  │ Streamlit │ 1.40.1 │
  ├───────────┼────────┤
  │ Pandas    │ 2.0.3  │
  ├───────────┼────────┤
  │ NumPy     │ 1.24.4 │
  ├───────────┼────────┤
  │ Plotly    │ 6.7.0  │
  ├───────────┼────────┤
  │ openpyxl  │ 3.1.5  │
  └───────────┴────────┘

  交付内容：
  - portable/python/ — 即开即用的 Python 3.8.10，含全部依赖
  - start.bat — 双击启动，用 %~dp0 相对路径，文件夹挪到任何位置都行

### 14.1 三种部署方式对比

| 方式 | 适合人群 | 优点 | 缺点 |
|------|---------|------|------|
| **Python 直接运行** | 有 Python 基础的开发者 | 方便调试和修改 | 需要装 Python |
| **打包为 EXE** | 最终用户 | 双击运行，无需安装任何东西 | 文件较大（约 200MB） |
| **Docker 镜像** | IT 管理员批量部署 | 环境完全一致 | 需要 Docker 知识 |

### 14.2 推荐分发流程

```
开发完成 → 用 PyInstaller 打包为单个 .exe
         → 将 .exe + data/ 目录 + 使用说明.pdf 打包为 .zip
         → 用户解压后双击 .exe 即可使用
```

### 14.3 数据备份

- 软件自动在每次导入新数据前备份旧数据库
- 备份文件保存在 `data/backups/` 目录，命名格式：`sales_data_YYYYMMDD_HHMMSS.db`
- 建议每周手动将 `data/` 目录复制一份到其他位置（如 U 盘）

---

## 15. TODO

- [ ] 综合看板页面添加“最新咨询”功能

 

- [ ] “国补核销预警”功能待实现



- [ ] 使用引导优化

---

## 附录A：requirements.txt

```text
# 核心依赖
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.17.0
openpyxl>=3.1.0

# 可选依赖
# pyecharts>=2.0.0          # 如需中国地图离线使用（Plotly 地图需要联网）
# geopandas>=0.14.0         # 如需高级地理数据处理
# pyinstaller>=6.0.0        # 打包为 EXE（开发完成后使用）
# pytest>=7.0.0             # 单元测试（开发阶段使用）
```

## 附录B：一键启动脚本

### Windows（`start.bat`）

*已修改为直接使用便携版 Python

```batch
@echo off
chcp 65001 >nul
title 家电销售数据可视化看板

echo ===================================
echo   家电销售数据可视化看板
echo ===================================
echo.

:: 检查 Python 是否安装
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo [错误] 未检测到 Python，请先安装 Python 3.8+
    echo 下载地址：https://www.python.org/downloads/
    pause
    exit /b 1
)

:: 创建虚拟环境（首次运行）
if not exist "venv\" (
    echo [首次运行] 正在创建虚拟环境...
    python -m venv venv
    echo [进度] 正在安装依赖包...
    call venv\Scripts\activate.bat
    pip install -r requirements.txt
) else (
    call venv\Scripts\activate.bat
)

:: 启动应用
echo [启动] 正在启动看板...
start http://localhost:8501
streamlit run main.py

pause
```

### macOS/Linux（`start.sh`）

```bash
#!/bin/bash
echo "==================================="
echo "  家电销售数据可视化看板"
echo "==================================="

# 检查 Python
if ! command -v python3 &> /dev/null; then
    echo "[错误] 未检测到 Python3，请先安装"
    exit 1
fi

# 创建虚拟环境（首次运行）
if [ ! -d "venv" ]; then
    echo "[首次运行] 正在创建虚拟环境..."
    python3 -m venv venv
    echo "[进度] 正在安装依赖包..."
    source venv/bin/activate
    pip install -r requirements.txt
else
    source venv/bin/activate
fi

# 启动
echo "[启动] 正在启动看板..."
open http://localhost:8501 2>/dev/null || xdg-open http://localhost:8501 2>/dev/null
streamlit run main.py
```

## 附录C：Excel 数据模板字段说明

提供给最终用户的 Excel 模板说明（可直接复制到 Excel 第一行作为表头）：

```
销售日期 | 订单编号 | 客户姓名 | 客户电话 | 商品名称 | 品类 | 品牌 | 型号 | 数量 | 金额 | 国补金额 | 核销状态 | 销售渠道 | 地址
```

**快捷提示**：如果数据只有最基础的几列（销售日期 + 金额 + 商品名称），也能正常导入和使用，只是回购分析、热力图等高级功能无法生效。

---

> **文档版本**：v1.0 | **最后更新**：2025-05-15 | **基于**：原《家电销售数据可视化看板软件开发方案》优化
